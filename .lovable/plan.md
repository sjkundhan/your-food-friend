
## Plan: Change App Icon to 🍔 Burger Emoji

**Approach**: Create an SVG favicon with the burger emoji and update `index.html` to use it, also updating the page title to "Foodie".

**Changes:**
1. `public/favicon.svg` — Create an SVG file that renders the 🍔 emoji as the favicon
2. `index.html` — Update `<title>` to "Foodie", add `<link rel="icon">` pointing to the new SVG favicon, and update OG/meta tags
